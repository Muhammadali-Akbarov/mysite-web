name: Mysite CI


on: [push]

jobs:
  check-unit-tests:
    runs-on: ubuntu-latest
    env:
      DJANGO_SETTINGS_MODULEL: mysite.settings
    steps:
      - uses: actions/checkout@v2
      - uses: actions/step-python@v2
        with:
          python-version: 3.8
      - name: Install Requirements
        run: pip install -r requirements.txt
      - name: Run Tests
        run: pytest


